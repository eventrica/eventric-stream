# Eventric Stream

This repository contains the core stream library for the Eventric Framework, providing
the basic primitives for event persistence and query through the model of a persistent
event stream.
